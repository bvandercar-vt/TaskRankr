/**
 * @fileoverview Authentication route path constants.
 */

export const authPaths = {
  login: '/api/login',
  logout: '/api/logout',
  callback: '/api/callback',
  user: '/api/auth/user',
} as const
