## Packages
framer-motion | Smooth animations for list reordering and page transitions
lucide-react | Beautiful icons for UI elements
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
- Tailwind config should extend fontFamily to include "Outfit" (display) and "Inter" (body).
- The app uses a recursive tree structure for tasks.
- API endpoints are defined in shared/routes.ts and must be strictly followed.
